machine:
  ruby:
    version: 2.3.0

test:
  pre:
    - ./script/cibuild

deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync _site/ s3://richland-main.s3-website.us-east-2.amazonaws.com --delete
