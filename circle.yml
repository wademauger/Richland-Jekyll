machine:
  ruby:
    version: 2.3.0

test:
  pre:
    - ./script/cibuild

deployment:
  prod:
    branch: master
    commands:
      - aws s3 cp _site/ s3://richland-main/ --recursive --region us-east-2 
