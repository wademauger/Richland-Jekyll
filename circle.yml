machine:
  ruby:
    version: 2.3.0
    
test:
  pre:
    - ./script/cibuild

deployment:
  automerge:
    branch: master
    commands:
      - echo "DEPLOYING..."
      - scp -P 2222 -r _site richboro@richlandborough.org:~/public_html/DevDeploy
#      - ssh -p 2222 richboro@richlandborough.org "cd Dev/; git pull; jekyll b; exit"
