machine:
  ruby:
    version: 2.3.0
    
test:
  pre:
    - ./script/cibuild

deployment:
  automerge:
    branch: master
    commands:
      - echo "DEPLOYING..."
      - scp -P 2222 richboro@richlandborough.org -r _site/
#      - ssh -p 2222 richboro@richlandborough.org "cd Dev/; git pull; jekyll b; exit"
