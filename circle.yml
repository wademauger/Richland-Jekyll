machine:
  ruby:
    version: 2.3.0

test:
  pre:
    - ./script/cibuild

deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync _site/ http://richland-main.s3.amazonaws.com/ --delete
